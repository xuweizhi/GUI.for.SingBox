# Bind locally by default. Change to 0.0.0.0:18080 if you need remote access.
WEBUI_LISTEN=127.0.0.1:18080

# Leave empty to let the app auto-generate a token on non-loopback binds.
# For any remote bind, setting an explicit token is strongly recommended.
WEBUI_TOKEN=change-me
